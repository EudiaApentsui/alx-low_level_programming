#include "main.h"
#include <stdio.h>

/**
 * add - a function that adds two integers and returns the result
 *
 * @a: para 1
 * @b: para 2
 *
 * Return: add
 */

int add(int a, int b)
{
	return (a + b);
}
